import React from 'react';
import './ConfigureNamespace.css';

const ConfigureNamespace = () => (
  <div className="ConfigureNamespace" data-testid="ConfigureNamespace">
    ConfigureNamespace Component
  </div>
);

export default ConfigureNamespace;
